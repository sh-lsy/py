"""
进阶篇 01: 类和对象 (Classes and Objects) - 大白话版

【核心概念】
1. 类 (Class): 就是图纸、模具。
2. 对象 (Object): 就是根据图纸造出来的房子、用模具印出来的饼干。
3. __init__: 生产车间的初始化流水线，给新产品贴标签（名字、颜色）。
4. self: 就是“我自己”。对象在说“我的名字”、“我的颜色”。
"""

# ==========================================
# 1. 定义一个类 (画图纸)
# ==========================================
class Cat:
    """
    这是一个猫的类（图纸）。
    它规定了：
    1. 每只猫都有名字和颜色 (属性)
    2. 每只猫都会叫 (方法)
    """

    # 初始化方法 (Constructor)
    # 当我们创建新猫时，Python 会自动调用这个方法
    def __init__(self, name, color):
        # self 表示“这只猫自己”
        # 给这只猫贴上“name”的标签，值是传入的 name
        self.name = name
        # 给这只猫贴上“color”的标签，值是传入的 color
        self.color = color
        print(f"🐱 叮！一只名叫【{self.name}】的{self.color}猫诞生了！")

    # 实例方法 (Instance Method)
    # 猫的行为
    def speak(self):
        # 在方法里，可以通过 self.name 获取这只猫的名字
        print(f"{self.name} 喵喵叫~")

    def eat(self, food):
        print(f"{self.name} 正在吃 {food}...")

# ==========================================
# 2. 创建对象 (根据图纸造猫)
# ==========================================
print("\n--- 1. 创建对象 ---")

# 使用 Cat 类，创建第一只猫 (对象)
# 这一步会自动调用 __init__，并把 "汤姆", "蓝色" 传进去
tom = Cat("汤姆", "蓝色")

# 使用 Cat 类，创建第二只猫 (对象)
jerry_cat = Cat("杰瑞的朋友", "灰色")


# ==========================================
# 3. 使用对象 (猫开始活动)
# ==========================================
print("\n--- 2. 对象在活动 ---")

# 访问属性 (查看标签)
print(f"第一只猫的名字是: {tom.name}")
print(f"第二只猫的颜色是: {jerry_cat.color}")

# 调用方法 (让猫做动作)
tom.speak()       # 汤姆叫
jerry_cat.speak() # 另一只猫叫

tom.eat("小鱼干") # 汤姆吃东西


# ==========================================
# 4. 常见误区 (Pitfalls)
# ==========================================
print("\n--- 3. 常见误区演示 ---")

# 误区 1: 忘记加 self
# 在类的方法里，如果要访问属性，必须用 self.xxx，不能直接写 xxx
# 错误写法: print(name) -> NameError
# 正确写法: print(self.name)

# 误区 2: 类 vs 对象
# Cat 是图纸，tom 是真猫。
# 你不能让图纸叫: Cat.speak() -> 报错 (除非传一个对象进去)
try:
    Cat.speak()
except TypeError as e:
    print(f"❌ 报错了: {e}")
    print("原因: 图纸(Class)本身不会叫，必须是具体的猫(Object)才会叫。")

